<% layout('/layouts/boilerplate') -%>
<body>
  <div class="row">
    <div class="col-sm-6 mb-3 mb-sm-0 offset-3 mt-5">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">Delete Listing</h5>
          <p class="card-text">Do you want to delete this listing?</p>
          

          <div class="row">
            <!-- Cancel button -->
          <div class="col-2">
            <form action="/listings/<%= listing._id  %>" method="GET">
              <button class="btn btn-primary">Cancel</button>
            </form>
          </div>
          
          <!-- delete button -->
          <div class="col-4">
            <form
            action="/listings/<%= listing._id %>?_method=DELETE"
            method="POST"
          >
            <button class="btn btn-danger">Yes, delete.</button>
          </form>
          </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
